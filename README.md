# macChanger
Simple Python script to randomly change the MAC address of a network interface on Linux. Retrieves the current MAC, generates a random one, applies it, and prints both old and new MACs. Intended for learning networking and Python subprocess handling.
